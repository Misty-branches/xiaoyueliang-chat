name: xiayue_chat
description: "遐悦聊天 - A lightweight custom chat client for chatting with 遐"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.4.0

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  flutter_markdown: ^0.7.1
  intl: ^0.19.0
  uuid: ^4.3.3
  file_picker: ^8.0.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
